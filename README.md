# A library to handle Hith Throughput sequencing data stored in a database.
Suppose we have a database table with a schema similar to a SAM file. This
library provides a convenient interface to iterate over the records in the
database and perform custom queries.
